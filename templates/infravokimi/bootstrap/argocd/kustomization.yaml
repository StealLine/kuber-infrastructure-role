apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configs

helmGlobals:
  chartHome: charts

helmCharts:
  - name: argo-cd
    releaseName: argocd
    valuesFile: configs/values.yaml
    namespace: argocd
