# Strategic merge patch for voki-comments-service in production
# Uncomment / adjust as needed
apiVersion: apps/v1
kind: Deployment
metadata:
  name: voki-comments-service
spec:
  template:
    spec:
      containers:
        - name: voki-comments-service
          resources:
            requests:
              memory: "128Mi"
              cpu: "25m"
            limits:
              memory: "512Mi"
              cpu: "500m"
