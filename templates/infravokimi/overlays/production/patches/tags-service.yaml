# Strategic merge patch for tags-service in production
# Uncomment / adjust as needed
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tags-service
spec:
  template:
    spec:
      containers:
        - name: tags-service
          resources:
            requests:
              memory: "128Mi"
              cpu: "25m"
            limits:
              memory: "512Mi"
              cpu: "500m"
