# Strategic merge patch for user-profiles-service in production
# Uncomment / adjust as needed
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-profiles-service
spec:
  template:
    spec:
      containers:
        - name: user-profiles-service
          resources:
            requests:
              memory: "128Mi"
              cpu: "25m"
            limits:
              memory: "512Mi"
              cpu: "500m"
