# Strategic merge patch for tags-service in staging
# Uncomment / adjust as needed
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tags-service
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: tags-service
          image: "{{ gitlab_container_registry }}/tags:main-stable"
          resources:
            requests:
              memory: "128Mi"
              cpu: "10m"
            limits:
              memory: "512Mi"
              cpu: "500m"
