# Strategic merge patch for voki-comments-service in staging
# Uncomment / adjust as needed
apiVersion: apps/v1
kind: Deployment
metadata:
  name: voki-comments-service
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: voki-comments-service
          image: "{{ gitlab_container_registry }}/voki-comments:main-stable"
          resources:
            requests:
              memory: "128Mi"
              cpu: "10m"
            limits:
              memory: "512Mi"
              cpu: "500m"
