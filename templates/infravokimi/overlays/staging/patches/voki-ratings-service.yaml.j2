# Strategic merge patch for voki-ratings-service in staging
# Uncomment / adjust as needed
apiVersion: apps/v1
kind: Deployment
metadata:
  name: voki-ratings-service
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: voki-ratings-service
          image: "{{ gitlab_container_registry }}/voki-ratings:main-stable"
          resources:
            requests:
              memory: "128Mi"
              cpu: "10m"
            limits:
              memory: "512Mi"
              cpu: "500m"
